// 9. Um sistema precisa exibir uma contagem regressiva antes de encerrar uma
// execução automaticamente.
// Crie um algoritmo que simule um contador regressivo de 10 até 0.

function contagemRegressiva() {
    for (i = 10; i >= 0; i --){
        console.log(i)
    }
}