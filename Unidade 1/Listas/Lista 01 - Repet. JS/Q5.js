// 5. Um sistema de lançamento precisa realizar uma contagem regressiva antes de
// iniciar uma operação crítica.
// Utilize a estrutura do...while para exibir números de 10 até 1 no console.

function contagem(){
    contador = 10
        do {
            console.log(`O sistema começará em ${contador}`)
            contador--
            } 
        while (contador >= 0){
    };
}
